const state = {
    cards: [],
    status: ''
}

const mutations = {
};

const actions = {
};

const getters = {
};

export const card = {
    namespaced: true,
    state,
    mutations,
    actions,
    getters
};